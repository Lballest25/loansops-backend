handler: src/users/post_user/handler.lambda_handler
description: Creates a new user (ADMIN only)
events:
  - http:
      path: users
      method: post
      authorizer:
        type: COGNITO_USER_POOLS
        authorizerId: !Ref ApiGatewayAuthorizer
